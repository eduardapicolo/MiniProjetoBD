def sendMessage(from, to, message, key, db):
    if len(message) >= 50:
        encrypted_message = encrypt_message() #adicionar função de criptografia!!!
        db.Messsages.insert({
            "from": from,
            "to" : to,
            "message" : encrypted_message.decode(),
            "timestamp" : datetime.now()
        })
    else:
        print("A mensagem precisa ter pelo menos 50 caracteres.")

        #ainda não está pronto
